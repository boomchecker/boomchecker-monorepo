---
"fw-bom-node": patch
---

This patch adds HTTP audio streaming capability to the BOM Node firmware, enabling real-time audio stream access via HTTP endpoints. The implementation introduces a new "pull" mode alongside the existing "push" mode, allowing clients to receive live audio data as a WAV stream.
