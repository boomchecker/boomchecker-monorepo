# LaTeX auxiliary files to ignore
*.aux
*.log
*.out
*.bbl
*.blg
*.toc
*.synctex.gz

# Python gitignores
*.pyc

# Retraining artifacts reproducible via seeded training (task retrain*:train:all);
# aggregated metrics (per_seed_metrics, summaries) stay committed.
retraining/retrain*/models/
retraining/retrain*/results/**/per_sample_seed*.csv
retraining/retrain*/results/unseen_snr_per_sample_seed*.csv