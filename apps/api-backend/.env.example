# API Backend Configuration

# Application environment
# Options: development, production
# Default: production (release mode)
APP_ENV=development

# Gin framework mode (overrides APP_ENV if set)
# Options: debug, release, test
# GIN_MODE=release

# Server configuration
PORT=8080

# Database configuration
DB_PATH=./data/boomchecker.db

# JWT Encryption Key (REQUIRED)
# Generate with: go run scripts/generate_keys.go
# CRITICAL: Keep this secret secure! If leaked, all JWT secrets are compromised
# Must be 32 bytes base64-encoded
JWT_ENCRYPTION_KEY=CHANGE_ME_GENERATE_WITH_SCRIPT

# Admin Authentication (REQUIRED for admin endpoints)
# Admin JWT secret - separate from node JWT encryption key for security
# Generate with: go run scripts/generate_keys.go
# Must be 32 bytes base64-encoded
ADMIN_JWT_SECRET=CHANGE_ME_GENERATE_WITH_SCRIPT

# Authorized admin email - only this email can request admin tokens
ADMIN_EMAIL=admin@example.com

# AWS SES Configuration (REQUIRED for admin authentication)
# AWS region for SES service (e.g., us-east-1, eu-west-1)
AWS_SES_REGION=us-east-1

# Email address that will appear in the "From" field of admin token emails
# This email must be verified in AWS SES
AWS_SES_FROM_EMAIL=noreply@boomchecker.com

# Note: AWS credentials are loaded from default provider chain
# (environment variables, shared credentials file, or IAM role on EC2)

# Logging
LOG_LEVEL=info

# ===================================
# Production deployment example:
# ===================================
# APP_ENV=production
# GIN_MODE=release
# PORT=8080
# DB_PATH=/var/lib/boomchecker/boomchecker.db
# JWT_ENCRYPTION_KEY=<base64-encoded-32-bytes>
