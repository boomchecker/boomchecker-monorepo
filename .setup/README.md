# Setup scripts and tasks

This directory contains setup tasks for the development environment.

## Available tasks

### Git Signing Setup

```bash
task git-signing
```

Configures Git to sign commits and tags using SSH key (`id_ed25519_signing`).
