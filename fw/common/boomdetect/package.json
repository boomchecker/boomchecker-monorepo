{
  "name": "fw-common-boomdetect",
  "version": "0.0.0",
  "private": true,
  "description": "Acoustic detection pipeline (MFCC, feature aggregation, classifier registry), shared between firmware and host tests"
}
